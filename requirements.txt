# ⚠️ 版本鎖死,不要改回 >= 浮動範圍。
# 2026-07-29 事故:原本寫 mcp[cli]>=1.2.0,Zeabur 某次重建 pip 抓到新版 mcp,
# 該版把 mcp.server.transport_security 搬走 → server.py import 失敗 → 走退路噴 ValueError
# → 容器無限重啟 → 對外 502。code 一行沒動,卻因「未鎖依賴 + 平台重建」整台崩潰。
# 這與 2026-06-12 畫圖 MCP(chechewolf-mcp)是同一顆未爆彈 —— 那次修了,語音這邊漏掉。
mcp[cli]==1.27.2
httpx>=0.27.0,<1.0
